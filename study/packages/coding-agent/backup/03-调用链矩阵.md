# Coding Agent 调用链矩阵

## 主调用链：从 CLI 到 Agent 响应

### 调用链 1: 启动流程

| 调用栈 | 代码位置 | 关键操作 | 类型/配置 | 疑问 & 答案 |
|--------|----------|----------|-----------|-------------|
| `main()` | `main.ts:316` | 解析 CLI 参数 | `Args` | Q: 支持哪些全局选项？ |
| └─> `parseArgs()` | `cli/args.ts` | 解析 `--model`, `--session` 等 | `ParsedArgs` | A: model, session, thinking, tools... |
| └─> `runMigrations()` | `migrations.ts` | 配置迁移 | - | Q: 迁移什么？ |
| └─> `SettingsManager.create()` | `settings-manager.ts` | 加载配置 | `SettingsManager` | A: 旧版本配置迁移到新格式 |
| └─> `ResourceLoader.reload()` | `resource-loader.ts` | 加载扩展/技能/主题 | `LoadExtensionsResult` | Q: 从哪里加载？ |
| └─> `createAgentSession()` | `sdk.ts:153` | **创建核心会话** | `CreateAgentSessionResult` | A: 全局 + 项目 + CLI 参数 |
| &nbsp;&nbsp;&nbsp;&nbsp;└─> `SessionManager.create()` | `session-manager.ts` | 创建或恢复会话 | `SessionManager` | Q: 如何决定恢复还是新建？ |
| &nbsp;&nbsp;&nbsp;&nbsp;└─> `new AgentSession()` | `agent-session.ts:395` | 初始化 AgentSession | `AgentSession` | A: 检查 session 文件是否存在 |
| &nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;└─> `_buildRuntime()` | `agent-session.ts:600` | 构建运行时环境 | - | Q: 运行时包括什么？ |
| &nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;└─> `loadExtensions()` | `extensions/runner.ts` | 加载扩展 | `ExtensionRunner` | A: 工具、命令、事件处理器 |
| └─> `InteractiveMode.run()` | `interactive/interactive-mode.ts` | 启动 TUI | - | Q: 其他模式呢？ |

---

### 调用链 2: 消息提交与处理

| 调用栈 | 代码位置 | 关键操作 | 类型定义 | 疑问 & 答案 |
|--------|----------|----------|----------|-------------|
| 用户输入 | TUI | 输入消息，按 Enter | - | Q: 如何支持多行？ |
| └─> `_submitMessage()` | `interactive-mode.ts:800` | 提交消息处理 | - | A: Shift+Enter |
| &nbsp;&nbsp;&nbsp;&nbsp;└─> `session.prompt()` | `agent-session.ts:550` | **AgentSession 入口** | `PromptOptions` | Q: 支持图片吗？ |
| &nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;└─> `emitBeforeAgentStart()` | `agent-session.ts:580` | 触发扩展事件 | `BeforeAgentStartEvent` | A: 支持 `ImageContent[]` |
| &nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;└─> `buildSystemPrompt()` | `system-prompt.ts` | 构建系统提示词 | `string` | Q: 包含哪些内容？ |
| &nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;└─> `this.agent.prompt()` | `pi-agent-core` | **核心 Agent 调用** | `Agent.prompt()` | A: AGENTS.md + skills + tools |
| &nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;└─> `streamSimple()` | `pi-ai` | LLM API 流式调用 | `Stream` | Q: 支持哪些 Provider？ |
| &nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;└─> 流式事件 | - | `message_update` | `MessageUpdateEvent` | A: Anthropic, OpenAI, Google... |
| &nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;└─> 工具调用 | - | `tool_call` | `ToolCall` | Q: 如何执行工具？ |
| &nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;└─> `executeToolCalls()` | `pi-agent-core` | 并行执行工具 | `ToolResult[]` | A: 默认并行，可配置串行 |
| &nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;└─> `tool.execute()` | `tools/*.ts` | 执行具体工具 | `ToolResult` | Q: 有哪些钩子？ |
| &nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;└─> `emitToolCall()` | `extensions/runner.ts` | 扩展拦截 | `ToolCallEvent` | A: before/after 钩子 |
| &nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;└─> `message_end` | - | 消息完成 | `MessageEndEvent` | Q: 何时触发压缩？ |
| &nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;└─> `shouldCompact()` | `compaction.ts` | 检查是否需要压缩 | `boolean` | A: 阈值 80% 或溢出 |
| &nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;└─> `compact()` | `compaction.ts` | **执行压缩** | `CompactionResult` | Q: 压缩策略？ |
| └─> UI 更新 | `interactive-mode.ts` | 渲染消息流 | `Component` | A: 摘要替换旧消息 |

---

### 调用链 3: 工具执行流程

| 调用栈 | 代码位置 | 关键操作 | 类型定义 | 参数/返回值 |
|--------|----------|----------|----------|-------------|
| LLM 返回 | `pi-ai` | `tool_calls` 数组 | `ToolCall[]` | `id, name, arguments` |
| └─> `executeToolCalls()` | `pi-agent-core` | 并行执行 | `Promise<ToolResult[]>` | - |
| &nbsp;&nbsp;&nbsp;&nbsp;└─> `executeSingleTool()` | - | 单个工具执行 | - | - |
| &nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;└─> `setBeforeToolCall()` | `agent-session.ts:470` | 扩展前置钩子 | `hookResult` | 可拦截/修改 |
| &nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;└─> `tool.execute()` | `tools/*.ts` | **实际执行** | `ToolResult` | `content, details, isError` |
| &nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;├─> `readTool` | `tools/read.ts` | 读取文件 | `ReadToolInput` | `{file_path, offset, limit}` |
| &nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;├─> `writeTool` | `tools/write.ts` | 写入文件 | `WriteToolInput` | `{file_path, content}` |
| &nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;├─> `editTool` | `tools/edit.ts` | 编辑文件 | `EditToolInput` | `{old_string, new_string}` |
| &nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;└─> `bashTool` | `tools/bash.ts` | 执行命令 | `BashToolInput` | `{command, timeout}` |
| &nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;└─> `setAfterToolCall()` | `agent-session.ts:490` | 扩展后置钩子 | `ToolResultEvent` | 可修改结果 |
| └─> `tool_result` 消息 | `pi-agent-core` | 返回给 LLM | `ToolResultMessage` | - |

---

### 调用链 4: 扩展加载流程

| 调用栈 | 代码位置 | 关键操作 | 类型定义 | 说明 |
|--------|----------|----------|----------|------|
| `ResourceLoader.reload()` | `resource-loader.ts:200` | 重新加载所有资源 | - | 启动或 `/reload` 时 |
| └─> `discoverExtensions()` | `resource-loader.ts:250` | 发现扩展路径 | `string[]` | 全局 + 项目 + CLI |
| └─> `ExtensionRunner.loadExtensions()` | `extensions/runner.ts:100` | 加载扩展 | `LoadExtensionsResult` | - |
| &nbsp;&nbsp;&nbsp;&nbsp;└─> `import(extensionPath)` | - | 动态导入 ES 模块 | `ExtensionModule` | Q: 如何隔离？ |
| &nbsp;&nbsp;&nbsp;&nbsp;└─> `module.default(api)` | - | **执行扩展工厂** | `ExtensionAPI` | A: 同进程，通过 API 控制 |
| &nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;└─> `api.registerTool()` | `extensions/types.ts` | 注册工具 | `ToolDefinition` | - |
| &nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;└─> `api.registerCommand()` | `extensions/types.ts` | 注册命令 | `CommandConfig` | - |
| &nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;└─> `api.on(event, handler)` | `extensions/types.ts` | 订阅事件 | `ExtensionEvent` | - |

---

### 调用链 5: 会话压缩流程

| 调用栈 | 代码位置 | 关键操作 | 类型定义 | 说明 |
|--------|----------|----------|----------|------|
| 触发条件 | 多种 | 阈值/溢出/手动 | - | - |
| └─> `compact()` | `compaction.ts:100` | 压缩入口 | `CompactionResult` | - |
| &nbsp;&nbsp;&nbsp;&nbsp;└─> `prepareCompaction()` | `compaction.ts:150` | 准备压缩 | `CompactionPreparation` | - |
| &nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;└─> `findCutPoint()` | `compaction.ts:200` | 找切割点 | `number` | 保留最近的完整回合 |
| &nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;└─> `collectEntriesForSummary()` | `compaction.ts:250` | 收集待摘要条目 | `SessionEntry[]` | - |
| &nbsp;&nbsp;&nbsp;&nbsp;└─> `generateSummary()` | `compaction.ts:300` | 生成摘要 | `string` | 调用 LLM 总结 |
| &nbsp;&nbsp;&nbsp;&nbsp;└─> `sessionManager.addCompactionEntry()` | `session-manager.ts` | 添加压缩条目 | `CompactionEntry` | 保存到 JSONL |
| └─> 恢复执行 | `agent-session.ts` | 继续之前的操作 | - | 可能重试 |

---

### 调用链 6: 会话分支/分叉流程

| 调用栈 | 代码位置 | 关键操作 | 类型定义 | 说明 |
|--------|----------|----------|----------|------|
| `/tree` 命令 | `interactive-mode.ts:1200` | 打开树形视图 | - | - |
| └─> `TreeSelectorComponent` | `components/tree-selector.ts` | 选择节点 | `SessionEntry` | 可视化树形 |
| └─> `session.switchToEntry()` | `agent-session.ts:900` | 切换到指定节点 | - | - |
| &nbsp;&nbsp;&nbsp;&nbsp;└─> `emitSessionBeforeSwitch()` | `extensions/runner.ts` | 扩展钩子 | `