# pi-ai 核心概念

## 1. 消息系统 (Message System)

### 1.1 消息类型定义

```typescript
// 用户消息
interface UserMessage {
  role: "user";
  content: string | (TextContent | ImageContent)[];
  timestamp: number;  // Unix timestamp (ms)
}

// 助手消息（来自 LLM 的回复）
interface AssistantMessage {
  role: "assistant";
  content: (TextContent | ThinkingContent | ToolCall)[];
  api: Api;
  provider: Provider;
  model: string;
  responseId?: string;  // Provider 特定的消息 ID
  usage: Usage;         // Token 使用情况
  stopReason: StopReason;
  errorMessage?: string;
  timestamp: number;
}

// 工具调用结果
interface ToolResultMessage<TDetails = any> {
  role: "toolResult";
  toolCallId: string;
  toolName: string;
  content: (TextContent | ImageContent)[];
  details?: TDetails;
  isError: boolean;
  timestamp: number;
}

type Message = UserMessage | AssistantMessage | ToolResultMessage;
```

### 1.2 消息内容块

```typescript
// 文本内容
interface TextContent {
  type: "text";
  text: string;
  textSignature?: string;  // OpenAI Responses 的元数据
}

// 思考/推理内容
interface ThinkingContent {
  type: "thinking";
  thinking: string;
  thinkingSignature?: string;  // Google 的 opaque signature
  redacted?: boolean;  // 是否被安全过滤器屏蔽
}

// 图像内容
interface ImageContent {
  type: "image";
  data: string;       // base64 编码
  mimeType: string;   // "image/jpeg", "image/png"
}

// 工具调用
interface ToolCall {
  type: "toolCall";
  id: string;
  name: string;
  arguments: Record<string, any>;
  thoughtSignature?: string;  // Google 特定的签名
}
```

---

## 2. 对话上下文 (Context)

```typescript
interface Context {
  systemPrompt?: string;    // 系统提示词
  messages: Message[];      // 对话历史
  tools?: Tool[];           // 可用工具列表
}
```

**关键特性**：
- `Context` 是**纯 JSON 对象**，可序列化、可持久化、可跨 Provider 传递
- 支持在对话中途**切换模型**（例如从 GPT-4 切换到 Claude）
- 完整的上下文包含系统提示、用户消息、助手回复、工具调用结果

---

## 3. 工具系统 (Tool System)

### 3.1 工具定义

```typescript
import { Type, TSchema } from "@sinclair/typebox";

interface Tool<TParameters extends TSchema = TSchema> {
  name: string;
  description: string;
  parameters: TParameters;  // TypeBox Schema
}

// 示例：天气查询工具
const weatherTool: Tool = {
  name: "get_weather",
  description: "获取指定城市的天气",
  parameters: Type.Object({
    city: Type.String({ description: "城市名称" }),
    unit: Type.Optional(
      Type.StringEnum(["celsius", "fahrenheit"])
    )
  })
};
```

### 3.2 工具调用流程

```
User ──[stream/complete]──▶ LLM
                              │
                              ▼ (LLM 决定调用工具)
                              ┌─────────────┐
                              │ Tool Call   │
                              │ {name, args}│
                              └──────┬──────┘
                                     │
                                     ▼
                              [用户执行工具]
                                     │
                                     ▼
                              ┌─────────────┐
                              │ Tool Result │
                              │ {output}    │
                              └──────┬──────┘
                                     │
                                     ▼
                              [stream/complete]
                                     │
                                     ▼
User ◀──[Assistant Response]──── LLM
```

### 3.3 工具参数验证

```typescript
import { validateToolCall, validateToolArguments } from "@mariozechner/pi-ai";

// 验证工具调用
const validatedArgs = validateToolCall(tools, toolCall);

// 验证参数（已知工具）
const validatedArgs = validateToolArguments(tool, toolCall);
```

- 使用 **AJV** 进行 JSON Schema 验证
- 支持**类型强制转换**（如字符串转数字）
- 浏览器扩展环境自动**跳过验证**（CSP 限制）

---

## 4. 模型系统 (Model System)

### 4.1 模型接口

```typescript
interface Model<TApi extends Api> {
  id: string;           // 模型 ID，如 "gpt-4o"
  name: string;         // 显示名称
  api: TApi;            // API 类型
  provider: Provider;   // 提供商 ID
  baseUrl: string;      // API 基础 URL
  reasoning: boolean;   // 是否支持推理
  input: ("text" | "image")[];  // 支持的输入类型
  cost: {
    input: number;      // $/million tokens
    output: number;
    cacheRead: number;
    cacheWrite: number;
  };
  contextWindow: number;  // 上下文窗口大小
  maxTokens: number;      // 最大输出 tokens
  headers?: Record<string, string>;  // 自定义头
  compat?: OpenAICompletionsCompat;  // 兼容性设置
}
```

### 4.2 模型获取

```typescript
import { getModel, getModels, getProviders } from "@mariozechner/pi-ai";

// 获取指定模型（支持 TypeScript 类型推断）
const model = getModel("openai", "gpt-4o-mini");
// 返回类型: Model<"openai-completions">

// 获取提供商列表
const providers = getProviders();  // ["openai", "anthropic", "google", ...]

// 获取提供商的所有模型
const openaiModels = getModels("openai");
```

### 4.3 成本计算

```typescript
import { calculateCost } from "@mariozechner/pi-ai";

const usage = {
  input: 1000,
  output: 500,
  cacheRead: 200,
  cacheWrite: 100,
  totalTokens: 1800,
  cost: { input: 0, output: 0, cacheRead: 0, cacheWrite: 0, total: 0 }
};

const cost = calculateCost(model, usage);
// cost.total = 输入成本 + 输出成本 + 缓存读成本 + 缓存写成本
```

---

## 5. 流式事件系统 (Event Streaming)

### 5.1 事件类型

```typescript
type AssistantMessageEvent =
  // 流开始
  | { type: "start"; partial: AssistantMessage }
  
  // 文本块事件
  | { type: "text_start"; contentIndex: number; partial: AssistantMessage }
  | { type: "text_delta"; contentIndex: number; delta: string; partial: AssistantMessage }
  | { type: "text_end"; contentIndex: number; content: string; partial: AssistantMessage }
  
  // 思考块事件
  | { type: "thinking_start"; contentIndex: number; partial: AssistantMessage }
  | { type: "thinking_delta"; contentIndex: number; delta: string; partial: AssistantMessage }
  | { type: "thinking_end"; contentIndex: number; content: string; partial: AssistantMessage }
  
  // 工具调用事件
  | { type: "toolcall_start"; contentIndex: number; partial: AssistantMessage }
  | { type: "toolcall_delta"; contentIndex: number; delta: string; partial: AssistantMessage }
  | { type: "toolcall_end"; contentIndex: number; toolCall: ToolCall; partial: AssistantMessage }
  
  // 完成事件
  | { type: "done"; reason: "stop" | "length" | "toolUse"; message: AssistantMessage }
  | { type: "error"; reason: "aborted" | "error"; error: AssistantMessage };
```

### 5.2 事件流使用模式

```typescript
const stream = stream(model, context, options);

// 模式 1: for await 遍历事件
for await (const event of stream) {
  if (event.type === "text_delta") {
    process.stdout.write(event.delta);  // 增量输出
  } else if (event.type === "toolcall_end") {
    console.log("Tool call:", event.toolCall);
  }
}

// 模式 2: 等待最终结果
const message = await stream.result();
console.log(message.content);

// 模式 3: 手动推送事件（Provider 内部使用）
stream.push({ type: "text_delta", contentIndex: 0, delta: "Hello", partial: msg });
stream.end(message);
```

### 5.3 EventStream 实现机制

```typescript
// 底层通用事件流类
class EventStream<T, R = T> implements AsyncIterable<T> {
  private queue: T[] = [];
  private waiting: ((value: IteratorResult<T>) => void)[] = [];
  private done = false;

  push(event: T): void;
  end(result?: R): void;
  result(): Promise<R>;
  [Symbol.asyncIterator](): AsyncIterator<T>;
}

// 专用消息事件流
class AssistantMessageEventStream extends EventStream<AssistantMessageEvent, AssistantMessage> {
  // 自动识别 done/error 事件作为结束标记
}
```

**关键设计**：
- 支持**拉取模式**（async iterator）和**推送模式**（push）
- 内部队列管理未消费的 event
- waiting 列表管理等待的 consumer

---

## 6. Provider 架构

### 6.1 Provider 接口

```typescript
interface ApiProvider<TApi extends Api, TOptions extends StreamOptions> {
  api: TApi;
  stream: StreamFunction<TApi, TOptions>;
  streamSimple: StreamFunction<TApi, SimpleStreamOptions>;
}

type StreamFunction<TApi extends Api, TOptions extends StreamOptions> = (
  model: Model<TApi>,
  context: Context,
  options?: TOptions,
) => AssistantMessageEventStream;
```

### 6.2 Provider 懒加载机制

```typescript
// register-builtins.ts

// 1. 创建懒加载包装器
function createLazyStream<TApi extends Api>(
  loadModule: () => Promise<LazyProviderModule<TApi>>
): StreamFunction<TApi, StreamOptions> {
  return (model, context, options) => {
    const outer = new AssistantMessageEventStream();
    
    // 立即返回流，异步加载实际 Provider
    loadModule()
      .then((module) => {
        const inner = module.stream(model, context, options);
        forwardStream(outer, inner);  // 转发事件
      })
      .catch((error) => {
        outer.push({ type: "error", reason: "error", error: createErrorMessage(model, error) });
      });
    
    return outer;
  };
}

// 2. 注册 Provider
export const streamAnthropic = createLazyStream(loadAnthropicProviderModule);

export function registerBuiltInApiProviders(): void {
  registerApiProvider({
    api: "anthropic-messages",
    stream: streamAnthropic,
    streamSimple: streamSimpleAnthropic,
  });
  // ... 其他 Provider
}
```

**懒加载优势**：
- 按需加载 Provider 代码，减少启动时间
- 避免未使用的 Provider 依赖被加载
- 错误隔离（一个 Provider 加载失败不影响其他）

---

## 7. 选项系统 (Options)

### 7.1 基础选项

```typescript
interface StreamOptions {
  temperature?: number;           // 温度 (0-2)
  maxTokens?: number;             // 最大输出 tokens
  signal?: AbortSignal;           // 用于取消请求
  apiKey?: string;                // 覆盖环境变量的 API Key
  transport?: "sse" | "websocket" | "auto";  // 传输方式
  cacheRetention?: "none" | "short" | "long"; // 缓存保留
  sessionId?: string;             // 会话 ID（用于缓存）
  headers?: Record<string, string>; // 自定义 HTTP 头
  onPayload?: (payload: unknown, model: Model) => unknown; // 请求前修改 payload
  maxRetryDelayMs?: number;       // 最大重试等待时间
  metadata?: Record<string, unknown>; // 元数据（如 user_id）
}
```

### 7.2 Simple 选项

```typescript
interface SimpleStreamOptions extends StreamOptions {
  reasoning?: ThinkingLevel;      // "minimal" | "low" | "medium" | "high" | "xhigh"
  thinkingBudgets?: ThinkingBudgets; // 自定义推理预算
}

// 推理预算
interface ThinkingBudgets {
  minimal?: number;   // 默认 1024
  low?: number;       // 默认 2048
  medium?: number;    // 默认 8192
  high?: number;      // 默认 16384
}
```

### 7.3 Provider 特定选项

```typescript
// OpenAI Completions
interface OpenAICompletionsOptions extends StreamOptions {
  toolChoice?: "auto" | "none" | "required" | { type: "function"; function: { name: string } };
  reasoningEffort?: "minimal" | "low" | "medium" | "high" | "xhigh";
}

// Anthropic
interface AnthropicOptions extends StreamOptions {
  thinking?: boolean;
  thinkingBudget?: number;
}

// Google Gemini
interface GoogleOptions extends StreamOptions {
  search?: boolean;  // 启用 Google 搜索
}
```

---

## 8. OAuth 认证系统

### 8.1 支持的 OAuth Provider

| Provider | ID | 用途 |
|----------|-----|------|
| Anthropic | `anthropic` | Claude Pro/Max |
| GitHub Copilot | `github-copilot` | Copilot Chat |
| Google Gemini CLI | `google-gemini-cli` | Cloud Code Assist |
| Google Antigravity | `google-antigravity` | Gemini 3 / Claude via Cloud |
| OpenAI Codex | `openai-codex` | ChatGPT Plus/Pro |

### 8.2 OAuth 流程

```typescript
// 1. 获取 OAuth Provider
const provider = getOAuthProvider("anthropic");

// 2. 执行登录
const credentials = await provider.login({
  onAuth: (info) => {
    console.log("Open URL:", info.url);  // 显示授权 URL
  },
  onPrompt: async (prompt) => {
    return await askUser(prompt.message);  // 交互式提示
  },
  onProgress: (msg) => console.log(msg),  // 进度更新
});

// 3. 使用凭证获取 API Key
const apiKey = provider.getApiKey(credentials);

// 4. Token 刷新
const newCredentials = await provider.refreshToken(credentials);
```

### 8.3 CLI OAuth 登录

```bash
# 交互式登录
npx @mariozechner/pi-ai login

# 指定 Provider 登录
npx @mariozechner/pi-ai login anthropic

# 列出支持的 Provider
npx @mariozechner/pi-ai list
```

---

## 9. 停止原因 (Stop Reason)

```typescript
type StopReason = 
  | "stop"      // 正常完成
  | "length"    // 达到 max_tokens 限制
  | "toolUse"   // 需要调用工具
  | "error"     // 发生错误
  | "aborted";  // 用户取消
```

**使用场景**：
- `stop`: 对话正常结束
- `length`: 输出被截断，可能需要增加 maxTokens
- `toolUse`: 收到工具调用，需要执行工具并继续对话
- `error`: API 错误或网络错误
- `aborted`: 用户通过 AbortSignal 取消

---

## 10. Token 使用统计

```typescript
interface Usage {
  input: number;       // 输入 tokens
  output: number;      // 输出 tokens
  cacheRead: number;   // 缓存读取 tokens
  cacheWrite: number;  // 缓存写入 tokens
  totalTokens: number; // 总计
  cost: {
    input: number;     // 输入成本 ($)
    output: number;    // 输出成本 ($)
    cacheRead: number;
    cacheWrite: number;
    total: number;
  };
}
```

**注意**：
- 流式响应中，Usage 可能在流中间更新（某些 Provider）
- 最终 Usage 在 `done` 事件中确认
- Cost 通过 `calculateCost()` 基于模型定价计算
