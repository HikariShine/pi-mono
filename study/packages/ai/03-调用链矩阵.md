nbsp;└─> `createLazyStream` 返回的函数 | `register-builtins.ts:150` | - | A: 执行包装函数 |
| &nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;└─> `const outer = new AssistantMessageEventStream()` | - | - | A: 创建外层流 |
| &nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;└─> `loadModule().then(module => { ... })` | - | `Promise` | A: 异步加载 Provider |
| &nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;└─> `import("./anthropic.js")` | `register-builtins.ts:220` | 动态导入 | A: 真正加载模块 |
| &nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;└─> `module.streamAnthropic(model, context, options)` | - | - | A: 调用实际实现 |
| &nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;└─> `forwardStream(outer, inner)` | `register-builtins.ts:130` | - | A: 转发事件到外层流 |
| 返回 | `outer` (AssistantMessageEventStream) | - | A: 立即返回，异步加载 |

---

## 调用链 6: 消息验证 (validateToolCall)

### 代码追踪

| 调用栈 | 代码/事件 | 类型/结构定义 | 疑问 & 答案 |
|--------|-----------|---------------|-------------|
| LLM 返回 | `toolCall: { name: "get_weather", arguments: {...} }` | - | Q: 如何验证参数？ |
| 用户调用 | `validateToolCall(tools, toolCall)` | `validation.ts` | - |
| └─> `const tool = tools.find(t => t.name === toolCall.name)` | `validation.ts:10` | - | A: 查找工具定义 |
| └─> `if (!tool) throw new Error("Unknown tool")` | `validation.ts:15` | - | A: 检查工具存在 |
| └─> `ajv.validate(tool.parameters, toolCall.arguments)` | `validation.ts:20` | `AJV` | Q: 如何验证？ |
| &nbsp;&nbsp;&nbsp;&nbsp;└─> `tool.parameters` 是 TypeBox Schema | `types.ts` | `TSchema` | A: 转换为 JSON Schema |
| &nbsp;&nbsp;&nbsp;&nbsp;└─> `ajv.compile(schema)` | - | - | A: 编译验证器 |
| &nbsp;&nbsp;&nbsp;&nbsp;└─> `validate(arguments)` | - | - | A: 执行验证 |
| 返回 | `validatedArgs` 或 throw Error | - | A: 返回验证后的参数 |

---

## 关键调用链总结

| 场景 | 入口 | 核心路径 | 出口 |
|------|------|----------|------|
| **流式调用** | `stream()` | `resolveApiProvider → provider.stream → fetch → parseSSE → push events` | `AssistantMessageEventStream` |
| **非流式调用** | `complete()` | `stream → result() → accumulate → return message` | `AssistantMessage` |
| **Simple 模式** | `streamSimple()` | `mapSimpleOptions → provider.streamSimple → stream` | `AssistantMessageEventStream` |
| **工具调用** | `stream(tools)` | `transformTools → LLM → parse toolcall → push toolcall_end` | `ToolCall` 事件 |
| **懒加载** | `provider.stream` | `createLazyStream → loadModule → forwardStream` | `AssistantMessageEventStream` |
| **消息验证** | `validateToolCall()` | `find tool → ajv.validate → return validated` | `validatedArgs` 或 Error |

---

## 事件流生命周期

```
User                    pi-ai                    Provider                   LLM API
  │                       │                         │                         │
  │ stream(model, ctx)    │                         │                         │
  │──────────────────────▶│                         │                         │
  │                       │ resolveApiProvider()    │                         │
  │                       │────────────────────────▶│                         │
  │                       │                         │ fetch()                 │
  │                       │                         │────────────────────────▶│
  │                       │                         │                         │
  │                       │                         │◀────────────────────────│
  │                       │                         │ SSE stream              │
  │                       │◀────────────────────────│                         │
  │                       │ push({type:"start"})    │                         │
  │◀──────────────────────│                         │                         │
  │ for await (event)     │                         │                         │
  │◀──────────────────────│ push({type:"text_delta",│                         │
  │                       │         delta: "He"})   │                         │
  │◀──────────────────────│ push({type:"text_delta",│                         │
  │                       │         delta: "llo"})  │                         │
  │                       │ ...                     │                         │
  │◀──────────────────────│ push({type:"done"})     │                         │
  │                       │ end(message)            │                         │
  │◀──────────────────────│                         │                         │
  │ process event         │                         │                         │
```
